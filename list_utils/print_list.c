#include "../push_swap.h"

void	print_list(Node **listInit)
{
	// Node	*aux;

	// aux = *listInit;
	// while (aux)
	// {
	// 	printf("value: %d, index: %d\n", aux->value, aux->index);
	// 	aux = aux->next;
	// }
}
